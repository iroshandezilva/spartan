# Spartant requires pnpm. See AGENTS.md and the architecture document.
# The pnpm version itself is pinned by the root package.json "packageManager" field.

# Refuse to install when the Node version does not satisfy the engines field.
engine-strict=true
