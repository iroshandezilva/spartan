# Changelog

Every user-visible change to `@iroshandezilva/spartant`. Consumers read this to
decide whether to upgrade, and what upgrading costs them.

Package version: <!-- spartant-version -->0.0.0<!-- /spartant-version -->

That line tracks the package manifest. `pnpm release:prepare` rewrites it, and
`pnpm release:check` fails when it drifts. While it reads `0.0.0`, nothing has
been published.

## Unreleased

Changes that are merged but not released are not listed here. They live as
individual release notes in [`.changes/`](.changes/README.md), one file per
change, and become a dated section below when `pnpm release:prepare` runs.

Run `pnpm release:check` to see what is pending and which version it proposes.

<!-- releases -->
